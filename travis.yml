language: python
python:
  - "2.6"
  - "2.7"

# command to install dependencies
install:
  - pip install numpy
  - pip install -U scikit-learn 
# command to run tests
script:
  - make
  - make vocabulary
  - make training
  - make evaluate
  - make clean